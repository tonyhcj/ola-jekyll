collections:
  authors:
    output: true

defaults:
  - scope:
      path: ""
    values:
      layout: "default"

exclude:
  - assets/css/styles.css

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag
